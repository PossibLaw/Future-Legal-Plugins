{
  "description": "PossibLaw guardrails — blocks destructive commands and protects sensitive files",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/validate-bash.py"
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/protect-files.py"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/format-check.sh"
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "You are a completion validator. Evaluate whether all requested tasks were completed. If the assistant is waiting for user input, selection, or clarification as the next step, that counts as complete. If tasks remain incomplete or errors are unresolved, block.\n\nRespond with ONLY a raw JSON object on a single line, no markdown fences, no explanation:\n{\"decision\":\"approve\",\"reason\":\"all tasks completed\"}\nor\n{\"decision\":\"block\",\"reason\":\"description of incomplete work\"}"
          }
        ]
      }
    ]
  }
}
